ADMIN_EXTRACT_PROMPT = """\
당신은 문서 분석 전문가입니다.
아래는 문서에서 OCR로 추출한 텍스트입니다.

OCR 텍스트:
{ocr_text}

이 문서에 포함된 모든 필드를 추출하세요.

출력 예시:
{{
  "fields": [
    {{"name": "성명", "type": "string"}},
    {{"name": "생년월일", "type": "date"}},
    {{"name": "학번", "type": "string"}},
    {{"name": "학점", "type": "number"}},
    {{"name": "참여 공연 목록", "type": "array"}}
  ]
}}

필드 타입 규칙:
- string: 단일 텍스트 값
- number: 숫자 값
- date: 날짜 값
- array: 여러 항목이 나열되는 리스트 (예: 경력 목록, 공연 목록, 수상 내역 등)

반드시 위 JSON 형식으로만 응답하세요. 설명 없이 JSON만 출력하세요."""

ADMIN_CLEAN_FIELDS_PROMPT = """\
당신은 문서 분석 전문가입니다.
아래는 문서에서 OCR로 추출한 필드 목록입니다. OCR 오류로 인해 일부 필드명이 깨져있거나 실제 문서 필드가 아닐 수 있습니다.

추출된 필드 목록:
{fields_json}

다음 기준으로 진짜 필드만 남겨 정리하세요:
1. 실제로 사용자가 기재하는 정보 항목만 유지 (예: 성명, 생년월일, 주소, 금액)
2. 깨진 문자/특수기호/랜덤 문자열은 제거 (예: '극[Y[놓르[Y', 'Jo곰어외매')
3. 문서에 인쇄된 기관명/전화번호/고정 문구는 제거 (예: '한국전련공사', '3123유료', 'KBS콜센터')
4. 필드명을 자연스러운 한국어로 정규화 (예: '성 명' → '성명', '발 행 일' → '발행일')
5. 의미가 중복되는 필드는 하나만 유지하고 type은 원래 값 그대로 유지

출력 형식:
{{
  "fields": [
    {{"name": "성명", "type": "string"}},
    {{"name": "발행일", "type": "date"}}
  ]
}}

반드시 위 JSON 형식으로만 응답하세요. 설명 없이 JSON만 출력하세요."""

VLM_ADMIN_EXTRACT_PROMPT = """\
이 문서 이미지에 포함된 모든 필드를 추출하세요.
OCR 텍스트 없이 이미지를 직접 분석하여 필드를 추출합니다.

출력 예시:
{{
  "fields": [
    {{"name": "성명", "type": "string"}},
    {{"name": "생년월일", "type": "date"}},
    {{"name": "금액", "type": "number"}},
    {{"name": "참여 공연 목록", "type": "array"}}
  ]
}}

필드 타입 규칙:
- string: 단일 텍스트 값
- number: 숫자 값
- date: 날짜 값
- array: 여러 항목이 나열되는 리스트 (예: 경력 목록, 공연 목록)

실제로 사용자가 기재하는 정보 항목만 추출하세요.
기관명, 고정 문구, 도장 텍스트는 제외하세요.
반드시 위 JSON 형식으로만 응답하세요. 설명 없이 JSON만 출력하세요."""

VLM_USER_EVALUATE_PROMPT = """\
이 문서 이미지를 분석하고 아래 후보 스키마 중 가장 적합한 것을 선택한 후 필드 값을 추출하세요.

후보 스키마 목록:
{schemas}

1. 이미지를 직접 보고 가장 적합한 스키마를 선택하세요.
2. 선택한 스키마의 각 필드 값을 이미지에서 찾아 추출하세요.
3. 찾을 수 없거나 판독 불가인 필드는 null로 표시하세요.

출력 예시 (schema_id가 2인 경우):
{{
  "schema_id": 2,
  "fields": {{
    "성명": "홍길동",
    "생년월일": "1990-01-01",
    "참여 공연 목록": ["2022 정기연주회", "2023 봄 공연"]
  }}
}}

필드 타입별 추출 규칙:
- string/number/date: 단일 값으로 추출
- array: 문서에서 나열된 항목들을 리스트로 추출. 없으면 []

반드시 위 JSON 형식으로만 응답하세요. 설명 없이 JSON만 출력하세요."""

USER_EVALUATE_PROMPT = """\
당신은 문서 분석 전문가입니다.
아래는 문서에서 OCR로 추출한 텍스트와 후보 스키마 목록입니다.

OCR 텍스트:
{ocr_text}

후보 스키마 목록:
{schemas}

1. 위 후보 스키마 중 이 문서에 가장 적합한 스키마를 선택하세요.
2. 선택한 스키마의 각 필드 값을 OCR 텍스트에서 찾아 추출하세요.
3. 찾을 수 없거나 판독 불가인 필드는 null로 표시하세요.

출력 예시 (schema_id가 2인 경우):
{{
  "schema_id": 2,
  "fields": {{
    "성명": "홍길동",
    "생년월일": "1990-01-01",
    "학번": null,
    "참여 공연 목록": ["2022 정기연주회", "2023 봄 공연", "2024 창작극"]
  }}
}}

필드 타입별 추출 규칙:
- string/number/date: 단일 값으로 추출
- array: 문서에서 나열된 항목들을 리스트로 추출. 없으면 []

반드시 위 JSON 형식으로만 응답하세요. 설명 없이 JSON만 출력하세요."""
